# AT-003: lostRevenueMock Endpoint

## Ziel
Neue HTTP-Funktion `lostRevenueMock`, die eine feste Zahl zurückgibt:
```json
{ "lostRevenue": 1234, "ts": "<ISO-8601>" }
ts dient der Nachvollziehbarkeit.

Scope
In-Scope: functions/index.js, functions/package.json (nur Scripts falls nötig)

Out-of-Scope: GCP-Deploy, echte Berechnung, DB-Zugriffe, weitere Endpunkte

Inputs
Keine.

Output
HTTP 200 JSON:

json
Code kopieren
{
  "lostRevenue": 1234,
  "ts": "2025-09-21T00:00:00.000Z"
}
Tests
Emulator starten wie in Runbooks.

Aufruf:

GET /recovery-engine/europe-west1/lostRevenueMock

Erwartet:

lostRevenue = 1234 (number)

ts = gültiges ISO-8601 (string)

Definition of Done
helloWorld unverändert lauffähig.

Nur genannte Dateien geändert.

Node-Engine bleibt 22. Region bleibt europe-west1.

Lokale Tests grün. CI bleibt grün.
